# ClawdSkills Environment Variables

# Database URL (default: SQLite in current directory)
DATABASE_URL=sqlite+aiosqlite:///./clawskills.db

# Optional: Bootstrap API key (will be created on first startup)
# API_KEYS_SEED=your_initial_api_key_here
